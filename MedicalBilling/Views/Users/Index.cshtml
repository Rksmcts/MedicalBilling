@model IEnumerable<MedicalBilling.User>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdminPanel.cshtml";
}

<h2>Users</h2>

<p>
    @Html.ActionLink("Create New", "Create",null,new { @class= "btn btn-info" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.accessLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.accessLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.uid },new { @class= "btn btn-warning btn-sm" }) @*|
    @Html.ActionLink("Details", "Details", new { id=item.uid }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.uid }, new { @class = "btn btn-danger btn-sm" })
        </td>
    </tr>
}

</table>
